spring.application.name=portifolio-bank

# Swagger UI apontando para seu YAML estático (se você estiver usando)
springdoc.swagger-ui.url=/swagger.yaml

# ========= DATASOURCE (MySQL) =========
spring.datasource.url=jdbc:mysql://localhost:3306/fadesp?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=meninoJesus
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ========= JPA / HIBERNATE =========
# create / create-drop / update / validate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Opcional: evita warnings com LOBs
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true